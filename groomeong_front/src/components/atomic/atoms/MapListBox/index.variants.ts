import { Variants } from "framer-motion";

export const MapListBoxVariants: Variants = {
  mouseOver: {
    backgroundColor: `rgba(32, 148, 255, .1)`,
  },
  mouseOut: {
    backgroundColor: "#fff",
  },
};
